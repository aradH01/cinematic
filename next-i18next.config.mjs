export const i18n = {
    locales: ['en', 'az', 'ru'], // Supported languages
    defaultLocale: 'en',        // Default language
    localeDetection: true,      // Auto-detect user language
};

export default i18n;
